package main

// Library imports
import (
	"fmt"
)

func main() {
		
	// Read in the JPEG image file.
	fmt.Print("Enter the filename of the source JPEG image: ")
	var fileName string
	fmt.Scanln(&fileName)
}
